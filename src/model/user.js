export default class User{
    constructor(ippis, firstName, lastName, email, password) {
        this.ippis = ippis;
        this.firstName = firstName;
        this.lastName = lastName;
        this.email = email;
        this.password = password;
    }
}